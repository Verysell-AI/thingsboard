{
  "name": "@platform/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright end-to-end tests against the running compose stack",
  "scripts": {
    "test": "echo 'e2e runs via make e2e (playwright test)' && exit 0",
    "e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ui-audit": "node tools/ui-audit.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.1",
    "typescript": "~5.9.3"
  }
}
